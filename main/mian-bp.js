/*
function buildCode(input) {
    const barcodes = deleteFrame(input);
    console.log(barcodes);
}

function deleteFrame(input) {
    input.split('').pop().shift();
    return input;
}

module.exports = {buildCode:buildCode};*/
